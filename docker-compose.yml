services:
  app:
    build: .
    container_name: air-dash
    restart: unless-stopped
    ports:
      - "3002:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/data/airvpn.db
    volumes:
      - airvpn_data:/data

volumes:
  airvpn_data:
